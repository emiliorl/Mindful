<?xml version="1.0" encoding="utf-8"?>
<!--
  Accessibility service configuration for MindShieldAccessibilityService.

  Only TYPE_WINDOW_STATE_CHANGED events are requested (Phase 2 needs no
  content retrieval), keeping the permission footprint minimal and
  privacy-respecting.
-->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="false"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100"
    android:settingsActivity=".MainActivity" />
